[project]
name = "pubmed-proto-generator"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
# Distribution is `xsd-former` on PyPI; it provides the `xsdformer` CLI.
dev = ["xsd-former==1.0.0"]
